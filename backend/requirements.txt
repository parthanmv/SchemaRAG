fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
psycopg[binary]>=3.2.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Phase 3: local embeddings + FAISS retrieval
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# Phase 4: SQL extraction/parsing/grounding over generated statements
sqlglot>=25.0.0

# Phase 4.1: Google Gemini LLM (official SDK; replaces direct Ollama usage)
google-genai>=2.0.0
# Imported directly by app.rag.llm.gemini for network-error classification
# (also a transitive dependency of google-genai)
requests>=2.31.0

# Testing
pytest>=8.2.0
httpx>=0.27.0
